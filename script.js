const heart = document.getElementById('heart');
const arrow = document.querySelector('.arrow');

/*
arrow.addEventListener('animationiteration', () => {
    setTimeout(() => {
        heart.classList.add('cracked');
    }, 1800); // Ajusta este valor para sincronizar con el momento en que la flecha pasa sobre el corazón
    setTimeout(() => {
        heart.classList.remove('cracked');
    }, 2300); // Ajusta este valor para finalizar la grieta
}); */